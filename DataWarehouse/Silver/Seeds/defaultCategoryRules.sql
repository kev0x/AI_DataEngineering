-- Category rules will be expanded after ETL parsing and source profiling.
-- The unknown fallback rule is seeded in defaultUnknownMembers.sql because factTransaction
-- depends on a valid categoryRuleKey = -1.

